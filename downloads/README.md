# Đặt file cài đặt app vào đây

- `AIFactory-Setup.exe` — bản cài Windows
- `AIFactory.dmg` — bản cài macOS

Tên file phải khớp với link trong `index.html` (mục Download).
Nếu đổi tên file, sửa lại `href` ở 2 nút Download trong `index.html`.
